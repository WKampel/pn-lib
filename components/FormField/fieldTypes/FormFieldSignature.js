import Signature from '../../Signature'

const FormFieldSignature = ({ value, onChange, label }) => {
  return <Signature value={value} onChange={onChange} label={label} />
}

export default FormFieldSignature
