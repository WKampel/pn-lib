import { Text } from 'react-native'

const FormFieldTitle = ({ label }) => {
  return <Text style={{ fontWeight: 'bold', textAlign: 'center', flex: 1 }}>{label}</Text>
}

export default FormFieldTitle
