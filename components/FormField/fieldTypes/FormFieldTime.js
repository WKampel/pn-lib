import TimeInput from '../../TimeInput'

const FormFieldTime = ({ value, onChange, label }) => {
  return <TimeInput value={value} onChange={onChange} label={label} />
}

export default FormFieldTime
