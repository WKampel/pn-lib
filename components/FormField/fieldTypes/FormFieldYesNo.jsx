import YesNoInput from '../../YesNoInput'

const FormFieldYesNo = ({ value, onChange, label }) => {
  return <YesNoInput value={value} onChange={onChange} label={label} />
}

export default FormFieldYesNo
