import TextInput from '../../TextInput'

const FormFieldTextInput = ({ value, onChange, label }) => {
  return <TextInput value={value} onChange={onChange} label={label} />
}

export default FormFieldTextInput
